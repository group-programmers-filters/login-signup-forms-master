import 'package:flutter/material.dart';

class provail_view extends StatelessWidget {
  const provail_view({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      
    );
  }
} 